{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a registered user's profile information, separate from authentication details.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the User entity (matches Firebase Auth UID)." },
        "username": { "type": "string", "description": "The user's unique, global @username for mentions." },
        "title": { "type": "string", "description": "An optional academic or professional title (e.g., 'Dr.', 'Prof.')." },
        "firstName": { "type": "string", "description": "The first name of the user." },
        "lastName": { "type": "string", "description": "The last name of the user." },
        "bio": { "type": "string", "description": "A short biography or description." },
        "avatarUrl": { "type": "string", "format": "uri", "description": "URL to the user's profile picture." },
        "avatarHint": { "type": "string", "description": "AI hint for the avatar image." },
        "openForWork": { "type": "boolean", "description": "Whether the user is available for work opportunities." },
        "category": { "type": "string", "description": "A category or professional title for the user, e.g., 'Photographer', 'Tech Enthusiast'." },
        "socialLinks": {
          "type": "array",
          "description": "A list of social media links for the user.",
          "items": {
            "type": "object",
            "properties": {
              "platform": { "type": "string", "description": "The social media platform name (e.g., 'instagram', 'x')." },
              "url": { "type": "string", "format": "uri", "description": "The URL to the user's profile on the platform." }
            },
            "required": ["platform", "url"]
          }
        },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "createdAt", "updatedAt"]
    },
    "Page": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Page",
      "type": "object",
      "description": "Represents a single, user-created 'Bento' page with all its content and appearance settings.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Page entity." },
        "ownerId": { "type": "string", "description": "The UID of the user who owns this page." },
        "slug": { "type": "string", "description": "The unique, user-defined URL slug for the page." },
        "title": { "type": "string", "description": "The title of the page." },
        "status": { "type": "string", "enum": ["draft", "published"], "description": "The visibility status of the page." },
        "fontFamily": { "type": "string", "description": "The CSS font-family for the page." },
        "backgroundImage": { "type": "string", "format": "uri" },
        "backgroundColor": { "type": "string" },
        "primaryColor": { "type": "string" },
        "accentColor": { "type": "string" },
        "foregroundColor": { "type": "string" },
        "cardColor": { "type": "string" },
        "cardForegroundColor": { "type": "string" },
        "borderRadius": { "type": "number" },
        "borderWidth": { "type": "number" },
        "borderColor": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "ownerId", "slug", "title", "status", "createdAt", "updatedAt"]
    },
    "Link": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Link",
      "type": "object",
      "description": "Represents a single custom link or content block on a user's Page.",
      "properties": {
        "id": { "type": "string" },
        "pageId": { "type": "string", "description": "Reference to the Page this link belongs to." },
        "title": { "type": "string" },
        "orderIndex": { "type": "number" },
        "type": { "type": "string", "enum": ["link", "text", "spotify", "youtube", "article", "blog-overview", "image", "product", "profile"], "description": "The type of content block." },
        "url": { "type": "string", "format": "uri" },
        "thumbnailUrl": { "type": "string", "format": "uri" },
        "thumbnailHint": { "type": "string", "description": "AI hint for the thumbnail image." },
        "content": { "type": "string", "description": "Content for text-based cards or product descriptions." },
        "excerpt": { "type": "string", "description": "A short summary or excerpt for an article card." },
        "readingTime": { "type": "string", "description": "Estimated reading time for the article (e.g., '5 min read')." },
        "publicationDate": { "type": "string", "description": "The publication date of the article (e.g., 'Jan 1, 2024')." },
        "showCreationDate": { "type": "boolean", "description": "Whether to show the creation date on the blog overview card." },
        "showExcerpts": { "type": "boolean", "description": "Whether to show post excerpts on the blog overview card." },
        "price": { "type": "string", "description": "The price of the product (e.g., '$19.99')." },
        "mentionedUserId": { "type": "string", "description": "The ID of the User being mentioned or featured." },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "pageId", "title", "orderIndex", "type", "createdAt", "updatedAt"]
    },
    "Post": {
      "title": "Post",
      "type": "object",
      "description": "Represents a single blog post written by a user.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Post entity." },
        "pageId": { "type": "string", "description": "The ID of the Page this post is associated with." },
        "ownerId": { "type": "string", "description": "The UID of the user who owns this post." },
        "title": { "type": "string" },
        "content": { "type": "string", "description": "The full content of the blog post, likely in Markdown." },
        "slug": { "type": "string", "description": "A unique slug for the post's URL." },
        "status": { "type": "string", "enum": ["draft", "published"], "description": "The current status of the post." },
        "category": { "type": "string", "description": "The category of the blog post." },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "authorName": { "type": "string", "description": "The name of the post's author, denormalized for efficient queries." },
        "authorAvatarUrl": { "type": "string", "format": "uri", "description": "The URL of the author's avatar, denormalized." },
        "authorPageSlug": { "type": "string", "description": "The URL slug of the author's page, denormalized." }
      },
      "required": ["id", "pageId", "ownerId", "title", "content", "slug", "status", "createdAt", "updatedAt"]
    },
    "SlugLookup": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SlugLookup",
      "type": "object",
      "description": "A lookup document to map a unique page URL slug to a Page ID for fast public lookups.",
      "properties": {
        "pageId": { "type": "string", "description": "The ID of the Page this slug points to." }
      },
      "required": ["pageId"]
    },
     "UsernameLookup": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UsernameLookup",
      "type": "object",
      "description": "A lookup document to map a unique @username to a User ID for fast public lookups and mentions.",
      "properties": {
        "userId": { "type": "string", "description": "The ID of the User this username points to." }
      },
      "required": ["userId"]
    }
  },
  "auth": {
    "providers": ["password", "google.com"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/user_profiles/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": { "$ref": "#/backend/entities/UserProfile" },
          "description": "Stores public profile information for a user. Document ID is the Firebase Auth UID."
        }
      },
      {
        "path": "/pages/{pageId}",
        "definition": {
          "entityName": "Page",
          "schema": { "$ref": "#/backend/entities/Page" },
          "description": "Stores all data for a single Bento page. Publicly readable, but only writable by the owner."
        }
      },
      {
        "path": "/pages/{pageId}/links/{linkId}",
        "definition": {
          "entityName": "Link",
          "schema": { "$ref": "#/backend/entities/Link" },
          "description": "Stores links for a specific page. Publicly readable, writable by the page owner."
        }
      },
      {
        "path": "/posts/{postId}",
        "definition": {
          "entityName": "Post",
          "schema": { "$ref": "#/backend/entities/Post" },
          "description": "Stores all data for a single blog post. Publicly readable if published, but only writable by the owner."
        }
      },
      {
        "path": "/slug_lookups/{slug}",
        "definition": {
          "entityName": "SlugLookup",
          "schema": { "$ref": "#/backend/entities/SlugLookup" },
          "description": "A public lookup table to map unique page URL slugs to page IDs."
        }
      },
      {
        "path": "/username_lookups/{username}",
        "definition": {
          "entityName": "UsernameLookup",
          "schema": { "$ref": "#/backend/entities/UsernameLookup" },
          "description": "A public lookup table to map unique @usernames to user IDs for mentions."
        }
      }
    ]
  }
}
